<%= form_for @reaction do |f| %>

<%= f.label :message_id %>
<%= f.collection_select  :message_id, Message.all, :id, :chatroom_id %>

<%= f.label :from_id %>
<%= f.collection_select  :from_id, User.all, :id, :name %>

<%= f.label :img_url %>
<%= f.text_field :img_url %>


<%= f.submit %>
<% end %>