
    <%if @chatroom.messages%>
        <%@chatroom.messages.each do |message|%>
            <p id="<%=message.to_or_from(@current_user)%>" style="width:<%=message.content.length*6%>px"><%=message.content%></p>
        <%end%>
    <%end%>
    <%=render "partials/new"%>
